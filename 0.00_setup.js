//setup file
var test = "Hello World";
console.log(test);

// Known as “the language of the web”; the most ubiquitous programming language

// Used for stack development, mobile applications; used to build frameworks and libraries

// Reads line by line, so code order matters

// A semicolon (;) goes at the end of a line of code

// Hoisting: what JavaScript does to try to help correct minor user errors (ex: missing semicolon, 
// missing “var” when declaring a variable, etc)
